---
import InfoCallout from './InfoCallout.astro';
import type { TournamentMetadata } from '../types';

interface Props {
  tournament: TournamentMetadata;
  leagueName: string;
}

const { tournament, leagueName } = Astro.props;
---

<InfoCallout emoji="👑" href={`/event/${tournament.tournamentId}`} linkText="View Event →">
  {
    tournament.winnerDisplayName && (
      <span>
        <span class="winner-name">{tournament.winnerDisplayName}</span> won the{' '}
        <span class="league-name">{leagueName}</span> top 8
        {tournament.winnerDeck && (
          <span>
            {' '}with <span class="deck-name">{tournament.winnerDeck}</span>
          </span>
        )}{' '}
        on <span class="date">{tournament.dateDisplay}</span>
      </span>
    )
  }
</InfoCallout>
